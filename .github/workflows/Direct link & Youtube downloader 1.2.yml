name: Direct link & Youtube downloader 1.2
on:
  workflow_dispatch:
    inputs:
      url:
        description: 'Enter "URL" "v/a" "video resolution/audio bitrate" "video FPS (optional)"'
        required: true

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Parse input (Is it YouTube?)
        id: parse
        run: |
          INPUT="${{ github.event.inputs.url }}"
          IFS=' ' read -ra TOKENS <<< "$INPUT"
          current_url=""
          current_opts=()
          > tasks.txt
          for token in "${TOKENS[@]}"; do
            if [[ "$token" =~ ^https?:// ]]; then
              if [ -n "$current_url" ]; then
                echo "$current_url ${current_opts[*]}" >> tasks.txt
              fi
              current_url="$token"
              current_opts=()
            else
              current_opts+=("$token")
            fi
          done
          if [ -n "$current_url" ]; then
            echo "$current_url ${current_opts[*]}" >> tasks.txt
          fi
          if grep -Eq '(youtube\.com/watch\?v=|youtu\.be/)' tasks.txt; then
            echo "IS_YOUTUBE=true" >> $GITHUB_OUTPUT
          else
            echo "IS_YOUTUBE=false" >> $GITHUB_OUTPUT
          fi

      - name: Install tools (YouTube only)
        if: steps.parse.outputs.IS_YOUTUBE == 'true'
        run: |
          if ! command -v yt-dlp &> /dev/null; then
            sudo curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
            sudo chmod a+rx /usr/local/bin/yt-dlp
          fi
          if ! command -v jq &> /dev/null; then
            sudo apt-get update -qq && sudo apt-get install -y -qq jq
          fi
          if ! command -v deno &> /dev/null; then
            curl -fsSL https://deno.land/install.sh | sh -s -- -y
            echo "$HOME/.deno/bin" >> $GITHUB_PATH
          fi
          sudo apt-get update -qq && sudo apt-get install -y -qq ffmpeg

      - name: Select format (YouTube only)
        if: steps.parse.outputs.IS_YOUTUBE == 'true'
        run: |
          set -euo pipefail
          export PATH="$HOME/.deno/bin:$PATH"
          python3 -c "
import subprocess, sys, json

YTDLP_BASE = 'yt-dlp --cookies cookies.txt --js-runtimes deno --remote-components ejs:npm --no-progress'
selected = []

with open('tasks.txt') as f:
    for line in f:
        line = line.strip()
        if not line:
            continue
        parts = line.split()
        url = parts[0]
        if not ('youtube.com/watch?v=' in url or 'youtu.be/' in url):
            continue
        video_id = url.split('v=')[-1].split('/')[0] if 'v=' in url else url.split('youtu.be/')[-1].split('/')[0]
        print(f'Processing YouTube URL: {url}')
        opts = parts[1:]
        if not opts:
            opts = ['v', 'max']
        groups = []
        i = 0
        while i < len(opts):
            t = opts[i].lower()
            if t not in ('v','a'):
                i += 1
                continue
            i += 1
            if i >= len(opts):
                groups.append((t, 'max', ''))
                continue
            val1 = opts[i]
            i += 1
            if t == 'v':
                if val1 == '2k': val1 = '1440'
                elif val1 == '4k': val1 = '2160'
            val2 = ''
            if t == 'v' and val1.isdigit() and i < len(opts) and opts[i].isdigit():
                val2 = opts[i]
                i += 1
            groups.append((t, val1, val2))
        seen = set()
        unique_groups = []
        for g in groups:
            if g not in seen:
                seen.add(g)
                unique_groups.append(g)
        tempfile = f'temp_{video_id}.json'
        try:
            subprocess.run(f'{YTDLP_BASE} -j \"{url}\" > {tempfile}', shell=True, check=True, stderr=subprocess.PIPE)
        except subprocess.CalledProcessError as e:
            print(f'ERROR: yt-dlp failed for {url}: {e.stderr.decode()}')
            continue
        with open(tempfile) as jf:
            data = json.load(jf)
        formats = data.get('formats', [])
        combined = [f for f in formats if f.get('vcodec') != 'none' and f.get('acodec') != 'none']
        audio_only = [f for f in formats if f.get('vcodec') == 'none' and f.get('acodec') != 'none']

        formats_to_download = {}
        for t, val1, val2 in unique_groups:
            if t == 'v':
                if val1 == 'all':
                    for f in combined:
                        formats_to_download[f['format_id']] = 'video'
                    continue
                if val1 == 'max':
                    fid = max(combined, key=lambda x: (x.get('height',0), x.get('fps',0)))['format_id']
                elif val1 == 'min':
                    fid = min(combined, key=lambda x: (x.get('height',0), x.get('fps',0)))['format_id']
                else:
                    target_height = int(val1)
                    target_fps = int(val2) if val2 else 0
                    same_height = [f for f in combined if f.get('height') == target_height]
                    if not same_height:
                        closest_height = min(combined, key=lambda x: abs(x.get('height',0)-target_height))['height']
                        same_height = [f for f in combined if f.get('height') == closest_height]
                    if target_fps > 0:
                        exact = [f for f in same_height if f.get('fps') == target_fps]
                        if exact:
                            fid = exact[0]['format_id']
                        else:
                            fid = max(same_height, key=lambda x: x.get('fps',0))['format_id']
                    else:
                        fid = max(same_height, key=lambda x: x.get('fps',0))['format_id']
                formats_to_download[fid] = 'video'
            else:
                if val1 == 'all':
                    for f in audio_only:
                        formats_to_download[f['format_id']] = 'audio'
                    continue
                if val1 == 'max':
                    fid = max(audio_only, key=lambda x: x.get('abr') or x.get('tbr') or 0)['format_id']
                elif val1 == 'min':
                    fid = min(audio_only, key=lambda x: x.get('abr') or x.get('tbr') or 0)['format_id']
                else:
                    target_br = int(val1)
                    best = min(audio_only, key=lambda x: abs((x.get('abr') or x.get('tbr') or 0) - target_br))
                    fid = best['format_id']
                formats_to_download[fid] = 'audio'

        entry = {
            'url': url,
            'video_id': video_id,
            'formats': formats_to_download
        }
        selected.append(entry)

with open('selected_formats.json', 'w') as outfile:
    json.dump(selected, outfile)
"

      - name: Download
        run: |
          mkdir -p temp_downloads
          is_youtube() { echo "$1" | grep -Eq '(youtube\.com/watch\?v=|youtu\.be/)'; }

          echo "[" > download_manifest.json
          first_url=true
          while IFS= read -r line; do
            url="${line%% *}"
            if ! is_youtube "$url"; then
              cd temp_downloads
              wget --progress=bar:force --content-disposition "$url"
              dl_file=$(ls -1)
              cd ..
              [ "$first_url" = true ] && first_url=false || echo "," >> download_manifest.json
              echo -n "{\"url\":\"$url\",\"is_youtube\":false,\"video_id\":\"\",\"files\":[{\"filename\":\"$dl_file\",\"type\":\"direct\"}]}" >> download_manifest.json
            fi
          done < tasks.txt

          if [ -f selected_formats.json ]; then
            YTDLP_BASE="--cookies cookies.txt --js-runtimes deno --remote-components ejs:npm --no-progress"
            while read -r entry; do
              url=$(echo "$entry" | jq -r '.url')
              video_id=$(echo "$entry" | jq -r '.video_id')
              formats=$(echo "$entry" | jq -r '.formats')
              [ "$first_url" = true ] && first_url=false || echo "," >> download_manifest.json
              echo -n "{\"url\":\"$url\",\"is_youtube\":true,\"video_id\":\"$video_id\",\"files\":[" >> download_manifest.json
              first_file=true
              for fid in $(echo "$formats" | jq -r 'keys[]'); do
                ftype=$(echo "$formats" | jq -r ".[\"$fid\"]")
                yt-dlp $YTDLP_BASE -f "$fid" -o "temp_downloads/%(title)s_${fid}.%(ext)s" "$url"
                dl_file=$(ls temp_downloads/*_${fid}.* 2>/dev/null | head -n1)
                [ -z "$dl_file" ] && continue
                [ "$first_file" = true ] && first_file=false || echo -n "," >> download_manifest.json
                echo -n "{\"filename\":\"$(basename "$dl_file")\",\"type\":\"$ftype\"}" >> download_manifest.json
              done
              echo -n "]}" >> download_manifest.json
            done < <(jq -c '.[]' selected_formats.json)
          fi
          echo "]" >> download_manifest.json

      - name: Remux (YouTube video only)
        if: steps.parse.outputs.IS_YOUTUBE == 'true'
        run: |
          if [ -f download_manifest.json ]; then
            jq -r '.[] | select(.is_youtube == true) | .files[] | select(.type == "video") | .filename' download_manifest.json | while read -r fname; do
              cd temp_downloads
              echo "Remuxing $fname..."
              out="fixed_$fname"
              ffmpeg -i "$fname" -c copy "$out" -y
              if [ $? -eq 0 ]; then rm "$fname" && mv "$out" "$fname"; else echo "Remux failed for $fname"; fi
              cd ..
            done
          fi

      - name: Create ZIP & Split file
        run: |
          mkdir -p final_downloads
          jq -c '.[]' download_manifest.json | while read -r entry; do
            url=$(echo "$entry" | jq -r '.url')
            is_yt=$(echo "$entry" | jq -r '.is_youtube')
            if [ "$is_yt" = "false" ]; then
              fname=$(echo "$entry" | jq -r '.files[0].filename')
              cd temp_downloads
              zip -s 99m -r "../final_downloads/${fname}.zip" "$fname"
              cd ..
            else
              video_id=$(echo "$entry" | jq -r '.video_id')
              video_files=$(echo "$entry" | jq -r '.files[] | select(.type == "video") | .filename')
              audio_files=$(echo "$entry" | jq -r '.files[] | select(.type == "audio") | .filename')
              if [ -n "$video_files" ]; then
                mkdir -p temp_downloads/${video_id}_videos
                for vf in $video_files; do cp "temp_downloads/$vf" "temp_downloads/${video_id}_videos/"; done
                zip -s 99m -r "final_downloads/${video_id}_videos.zip" "temp_downloads/${video_id}_videos"
                rm -rf "temp_downloads/${video_id}_videos"
              fi
              if [ -n "$audio_files" ]; then
                mkdir -p temp_downloads/${video_id}_audios
                for af in $audio_files; do cp "temp_downloads/$af" "temp_downloads/${video_id}_audios/"; done
                zip -s 99m -r "final_downloads/${video_id}_audios.zip" "temp_downloads/${video_id}_audios"
                rm -rf "temp_downloads/${video_id}_audios"
              fi
            fi
          done

      - name: Upload to download folder
        env:
          BRANCH: ${{ github.ref_name }}
        run: |
          mkdir -p downloads
          if [ -d final_downloads ] && [ "$(ls -A final_downloads 2>/dev/null)" ]; then
            mv final_downloads/* downloads/
          fi
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git config http.postBuffer 524288000

          if [ ! -d downloads ] || [ -z "$(ls -A downloads 2>/dev/null)" ]; then
            echo "No files to upload."
            exit 0
          fi

          FILES=(downloads/*)
          total=${#FILES[@]}
          BATCH=19
          start=0

          while [ $start -lt $total ]; do
            end=$(( start + BATCH ))
            if [ $end -gt $total ]; then end=$total; fi
            git add "${FILES[@]:start:end-start}"
            git commit -m "Add parts $(($start+1))-$end [skip ci]"
            git push origin "$BRANCH"
            start=$end
          done

          echo "All files uploaded to the 'downloads' folder."
